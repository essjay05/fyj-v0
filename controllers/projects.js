// Require User and projects models
const User = ('../models/User'),
    Project = ('../models/User');

// Make project controls exportable
module.exports = {
    // Create Project

    // Index Projects

    // Show 1 Project

    // Update Project

    // Delete Project
}